# Threat Model

Formal threat model docs land here as each detector category (prompt injection, obfuscation, RAG poisoning, agent misuse, output leakage) is implemented and tested — not before, per the project's Authenticity Policy.
